import Contact from "../components/Contact";

export const metadata = {
  title: "Get in touch — Cell Context",
  description:
    "Have a question about the science, the trial, or LNAD⁺ itself? Our team reads every note.",
};

export default function ContactPage() {
  return <Contact />;
}
