# cult-ui-pro registry key — referenced by components.json (X-API-Key).
# Used ONLY at build/install time by `shadcn add @cult-ui-pro/...`.
# Copy this file to .env.local and fill in the real value (do NOT commit .env.local).
CULT_UI_PRO_API_KEY=
